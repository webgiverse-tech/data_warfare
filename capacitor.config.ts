import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.cosmicseahorseflip',
  appName: 'cosmic-seahorse-flip',
  webDir: 'dist'
};

export default config;
